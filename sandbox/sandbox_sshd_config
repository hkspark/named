#Add to /etc/ssh/sshd_config
#Make sure /etc/passwd has /bin/sh as last file (shell to login)
Match User jbezos
    ChrootDirectory /home/jbezos/
    ForceCommand /bin/sh
    AllowTcpForwarding no
    X11Forwarding no
    PermitTTY yes
